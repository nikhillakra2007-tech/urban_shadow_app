# UUS Delhi (Urban Shadow) — frontend environment template.
# Copy to `.env.local` (dev) or set at build time in your host.
# Variable NAMES only — never put real secrets in this file.

# Base URL of the FastAPI backend, NO trailing slash.
# Local dev:  http://localhost:8000
# Production: https://your-backend-host.example
NEXT_PUBLIC_API_URL=http://localhost:8000
