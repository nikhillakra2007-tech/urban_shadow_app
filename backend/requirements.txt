fastapi>=0.115
uvicorn[standard]>=0.30
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
# The delivered uus_model.pkl was serialized under XGBoost 2.x and cannot be
# deserialized by 3.x ("input stream corrupted") - keep the 2.x line.
xgboost>=2.1.4,<3
joblib>=1.3
pydantic>=2.7
python-dotenv>=1.0
